
import { Transaction, Budget, FixedExpense, AdvisorInsight, LanguageCode } from "../types";
export const AiAdvisor = {
    analyze: (t: Transaction[], b: Budget[], f: FixedExpense[], c: number, l: LanguageCode): AdvisorInsight[] => []
};
